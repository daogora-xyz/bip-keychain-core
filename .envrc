#!/usr/bin/env bash
# Automatically load the Nix development shell when entering this directory
# Requires: direnv (https://direnv.net/)
#
# Usage:
#   1. Install direnv: https://direnv.net/docs/installation.html
#   2. Add to your shell config: eval "$(direnv hook bash)" (or zsh/fish)
#   3. Run: direnv allow
#   4. cd into this directory - the Nix shell will load automatically

use flake

# Optional: Set additional environment variables
# export RUST_BACKTRACE=1
# export RUST_LOG=debug
